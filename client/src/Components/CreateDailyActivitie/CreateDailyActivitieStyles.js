import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    dailyactivities: {
        // textAlign: 'center',
        // background: 'blue',
        margin: '0 auto',
        height: 500,
        paddingTop: 50,
        paddingLeft: 50,
        border: '1px solid orange',
    }
}))